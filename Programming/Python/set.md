---
tags: [programming, python, python/features]
title: set
date created: Thursday, November 20th 2025, 10:14:11 pm
date modified: Sunday, March 1st 2026, 4:05:14 pm
---
# set
A `set` is a immutable list which must also only contain unique items, like `tuple`, it can store multiple values of different types. `set`s are also unordered

> [!NOTE]
>  `True` and `1` are the same thing in sets, so are `False` and `0`

You declare a set like this:

```python
a = {1, 2, 3}
b = set(DATA) # DATA can be either a list or a tuple

c = set() # empty set
```

#python #python/features #programming 